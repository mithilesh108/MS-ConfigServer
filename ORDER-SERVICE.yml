server:
  port: 8082
  servlet:
    context-path: /api
management:
  endpoint:
    refresh:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*" # refresh
ms:
  order:
    category:
      price: 12345
